#!/bin/zsh

SCRIPT="${0##*/}" 

SCRIPT="${SCRIPT/vars/}" 
[[ -n ${SCRIPT} ]] && POSTFIX="$(uppercase ${SCRIPT/vars/})" 
POSTFIX="${POSTFIX/-/_}"

CMD="vvars -t \${SHELL_VARS${POSTFIX}} ${@}"
#ecko "${CMD}" 118 88 > /dev/stdin
eval "${CMD}"
