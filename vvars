#!/bin/zsh

CURR_SHELL_VARS="${SHELL_VARS}"
startsWithDash ${1} && ([[ ${1/-/} = 't' ]] || [[ ${1/-/} = '-target' ]]) && [[ -r "${2}" ]] && CURR_SHELL_VARS="${2}" && shift 2

_vvars () {
	cat ${CURR_SHELL_VARS} | grep --color=auto $@
}
_vvars ${@}
